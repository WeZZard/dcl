//===--- OS.def - Triple Definition Meta-Programming ------------*- C++ -*-===//
//
// This source file is part of the DCL open source project
//
// Copyright (c) 2022 Li Yu-Long and the DCL project authors
// Licensed under Apache 2.0 License
//
// See https://github.com/dcl-project/dcl/LICENSE.txt for license information
// See https://github.com/dcl-project/dcl/graphs/contributors for the list of
// DCL project authors
//
//===----------------------------------------------------------------------===//

#ifndef OS
#define OS(NAME, PATTERN, DESCRIPTION)
#endif

OS(Unknown, *, Unknown OS)
OS(Ananas, Ananas, Ananas)
OS(CloudABI, CloudABI, CloudABI)
OS(Darwin, Darwin, Darwin)
OS(DragonFly, DragonFly, DragonFly)
OS(FreeBSD, FreeBSD, FreeBSD)
OS(Fuchsia, Fuchsia, Fuchsia)
OS(IOS, IOS, IOS)
OS(KFreeBSD, KFreeBSD, KFreeBSD)
OS(Linux, Linux, Linux)
OS(Lv2, Lv2, Lv2)
OS(MacOSX, MacOSX, MacOSX)
OS(NetBSD, NetBSD, NetBSD)
OS(OpenBSD, OpenBSD, OpenBSD)
OS(Solaris, Solaris, Solaris)
OS(Win32, Win32, Win32)
OS(ZOS, ZOS, ZOS)
OS(Haiku, Haiku, Haiku)
OS(Minix, Minix, Minix)
OS(RTEMS, RTEMS, RTEMS)
OS(NaCl, NaCl, NaCl)
OS(AIX, AIX, AIX)
OS(CUDA, CUDA, CUDA)
OS(NVCL, NVCL, NVCL)
OS(AMDHSA, AMDHSA, AMDHSA)
OS(PS4, PS4, PS4)
OS(ELFIAMCU, ELFIAMCU, ELFIAMCU)
OS(TvOS, TvOS, TvOS)
OS(WatchOS, WatchOS, WatchOS)
OS(Mesa3D, Mesa3D, Mesa3D)
OS(Contiki, Contiki, Contiki)
OS(AMDPAL, AMDPAL, AMDPAL)
OS(HermitCore, HermitCore, HermitCore)
OS(Hurd, Hurd, Hurd)
OS(WASI, WASI, WASI)
OS(Emscripten, Emscripten, Emscripten)

#ifdef OS
#undef OS
#endif
